Steps for running the package tiago_task(The steps for creating workspace and building package are ignored):

open a terminal, run(WORKSPACE is the path of workspace):
$ source WORKSPACE/devel/setup.bash
$ roslaunch tiago_task navigation.launch
Then the navigation will start automatically.
